# Zigbee2MQTT Configuration Example
# Copy this to your Zigbee2MQTT configuration.yaml if needed

# Note: This is an example configuration
# Actual configuration depends on your Zigbee2MQTT setup
# Most settings are auto-discovered when device pairs

# Device-specific settings (if needed)
device_options:
  water_flow_meter:
    friendly_name: "Water Flow Meter"
    retain: true
    
# Optional: Custom MQTT topics (default: zigbee2mqtt)
# advanced:
#   topic_prefix: zigbee2mqtt
#   last_seen: ISO_8601
#   legacy_api: false

# Optional: Device-specific overrides
# experimental:
#   output: json

